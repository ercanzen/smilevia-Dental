import { Link } from 'react-router-dom'
import { blogData } from '../shared/blogData'

export default function Blog() {
  return <main id="main-content" className="min-h-screen bg-[#f3f5fb] px-4 pb-24 pt-28 text-[#0a1130] md:px-8 md:pt-36"><div className="mx-auto max-w-[1220px]"><p className="eyebrow">Ratgeber für Patienten aus der Schweiz</p><h1 className="section-title mt-5">Zahnbehandlung in Antalya:<br/><span className="hero-script font-normal italic text-[#4f6fd6]">gut informiert</span> entscheiden.</h1><p className="mt-6 max-w-2xl text-[#596378]">Verständliche Informationen über Zahnimplantate, Veneers, Kostenfaktoren, Reiseplanung und Nachsorge. Medizinische Inhalte werden vor Veröffentlichung fachlich geprüft.</p><div className="mt-14 grid gap-5 md:grid-cols-2">{blogData.map((post,i)=><article key={post.slug} className={`glass group overflow-hidden rounded-[32px] ${i===0?'md:col-span-2':''}`}><Link to={`/blog/${post.slug}`} className="block overflow-hidden"><img src={post.image} alt={post.title} loading="lazy" className={`w-full object-cover transition duration-700 group-hover:scale-105 ${i===0?'h-72 md:h-[480px]':'h-72'}`}/></Link><div className="p-7 md:p-9"><p className="eyebrow">{post.category}</p><h2 className={`mt-4 font-black leading-tight ${i===0?'text-3xl md:text-5xl':'text-2xl md:text-3xl'}`}>{post.title}</h2><p className="mt-4 max-w-3xl text-sm leading-relaxed text-[#5b6478]">{post.description}</p><Link to={`/blog/${post.slug}`} className="mt-7 inline-flex text-sm font-black">Ratgeber lesen ↗</Link></div></article>)}</div><div className="mt-16 glass-dark rounded-[36px] bg-[#15265e]/55 p-8 text-white md:flex md:items-center md:justify-between md:p-12"><div><h2 className="text-3xl font-black">Haben Sie eine persönliche Frage?</h2><p className="mt-2 text-white/55">Wir antworten direkt und unverbindlich.</p></div><Link to="/iletisim" className="mt-6 inline-block rounded-full bg-[#4da8ff] px-7 py-4 font-bold text-[#0a1130] md:mt-0">Kontakt aufnehmen ↗</Link></div></div></main>
}
